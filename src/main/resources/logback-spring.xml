<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <property name="APP_NAME" value="VietRecruit"/>
    <property name="LOG_PATH" value="${user.home}/${APP_NAME}/logs"/>
    <property name="LOG_FILE" value="${LOG_PATH}/application.log"/>

    <appender name="CONSOLE"
              class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <charset>utf8</charset>
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss.SSS}|%thread|%X{traceId}|%level|%logger{36}|%msg%n
            </pattern>
        </encoder>
    </appender>

    <!-- Logstash appender: uncomment when logstash-logback-encoder dep is active in pom.xml -->
    <!--
    <springProfile name="prod">
        <appender name="LOG_STASH"
                  class="net.logstash.logback.appender.LogstashTcpSocketAppender">
            <destination>${LOGSTASH_HOST:-localhost}:5044</destination>
            <encoder charset="UTF-8"
                     class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
                <providers>
                    <timestamp>
                        <timeZone>UTC</timeZone>
                    </timestamp>
                    <pattern>
                        <pattern>
                            {
                            "vr_date": "%d{yyyy-MM-dd HH:mm:ss.SSS}",
                            "vr_thread": "%thread",
                            "vr_traceId": "%X{traceId}",
                            "vr_level": "%level",
                            "vr_logger": "%logger{40}",
                            "vr_message": "%msg%n"
                            }
                        </pattern>
                    </pattern>
                </providers>
            </encoder>
        </appender>
    </springProfile>
    -->

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <!-- <appender-ref ref="LOG_STASH"/> -->
    </root>

</configuration>
